import {useState, useEffect} from 'react';
import './App.css'
import axios from 'axios';

function BirdInfo(props: any) {
    const [showBird, setBird] = useState(props.isOpen)
    useEffect(() => {

    }, [])
}

export default BirdInfo